---
image: brunosb/semantic-delivery-gitlab:latest

stages:
  - release

publish:
  stage: release
  script:
    - git pull --tags
    - semantic-delivery-gitlab --token $GITLAB_TOKEN
  only:
    - master
